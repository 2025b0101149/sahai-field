# SahAI Field Environment Configuration

# Backend API Port
PORT=5000

# Optional MongoDB URI (if omitted or unreachable, backend safely uses local JSON file store)
# MONGODB_URI=mongodb://localhost:27017/sahai_field

# Frontend API URL (for production build / CORS)
FRONTEND_URL=http://localhost:5173

# Node Environment
NODE_ENV=development
